version: '3.9'
services:
  api:
    platform: linux/amd64
    build:
      context: ./api
      dockerfile: Dockerfile
    volumes:
      - ./api:/app
    env_file:
      - ./api/.env
    ports:
      - 8080:8080
